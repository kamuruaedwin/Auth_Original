<nav class="navbar navbar-expand-lg">
  <div class="container-fluid">
     <a class="navbar-brand" href="#"><strong>PesaFast</strong><br>Play Win Play</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation" color="blue">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        
         <% if user_signed_in? %>          
         
          <li class="nav-item">          
          <%= link_to "Deposit", deposits_new_path, data: { turbo_frame: "modal"} %>
          </li>
          <p>welcome <%=current_user.username%></p><br>
          <p>  balance <%=current_user.balance%></p>
         
          <% end %>
        
        
          <%if user_signed_in?%>
          <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            More
          </a>
          <ul class="dropdown-menu">
            <li><%= link_to "Withdraw", withdraws_new_path%></li>
            <li><hr class="dropdown-divider"></li>
            <%= button_to "Log out", logout_path, method: :delete%>
          </ul>
          <%end%>
        </li>
       
      </ul>
      
    </div>
  </div>
</nav>